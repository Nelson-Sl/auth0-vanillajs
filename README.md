# Auth0-Vanillajs

This is a sample single-page application for Auth0

## Pre-Defined Setup

Create an app in auth0 account, that will generate a pair of `VITE_AUTH0_DOMAIN` and `VITE_CLIENT_ID` pair value. Copy to `.env` file once it's all generated.